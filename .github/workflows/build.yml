# Superseded by push.yaml + build-project.yaml — this file can be deleted.
name: (Deprecated)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "This workflow is superseded by push.yaml and build-project.yaml."
